export const metadata = {
  title: 'Giftflow Pro — Premium add-on',
  description:
    'Giftflow Pro extends the free WordPress plugin with recurring donations for Stripe and PayPal, data export, and more. Compare Free vs Pro.',
};

export default function ProLayout({ children }) {
  return children;
}
