export const metadata = {
  title: 'Contact — Giftflow',
  description:
    'Get in touch about the Giftflow WordPress plugin, Pro licensing, support, or partnerships.',
};

export default function ContactLayout({ children }) {
  return children;
}
